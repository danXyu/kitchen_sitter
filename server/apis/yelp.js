/* API: Yelp, implements the necessary api calls to the yelp api in order to fetch the restaurants and
 * dining locations to return back to the user. This should only be defined on the server, hence the
 * necessary check for the meteor to be run on the server.
 */
if (Meteor.isServer) {
  Meteor.methods({

  });
}